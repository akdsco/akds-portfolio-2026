import { expect, test } from "vitest";

// Harness sanity check — proves Vitest + jsdom boot. Replaced by real tests in
// the next increment.
test("vitest runs", () => {
  expect(true).toBe(true);
});
